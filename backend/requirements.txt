# CliniQAI Backend Dependencies
# Python 3.11+

# ─── API FRAMEWORK ────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
httpx==0.27.0

# ─── DATA VALIDATION ──────────────────────────────────────────
pydantic==2.7.1
pydantic-settings==2.3.0

# ─── DATABASE ─────────────────────────────────────────────────
sqlalchemy[asyncio]==2.0.30
asyncpg==0.29.0                    # PostgreSQL async driver
alembic==1.13.1                    # Migrations
psycopg2-binary==2.9.9             # Sync driver (for migrations)

# ─── CACHE + MESSAGE QUEUE ────────────────────────────────────
redis[hiredis]==5.0.6
aiokafka==0.11.0                   # Async Kafka client
confluent-kafka==2.4.0             # Alternative Kafka client

# ─── FHIR ─────────────────────────────────────────────────────
fhirclient==4.2.0                  # FHIR R4 Python client
fhir.resources==7.1.0              # FHIR R4 resource models
requests-oauthlib==1.3.1           # SMART on FHIR OAuth2

# ─── VECTOR STORE ─────────────────────────────────────────────
qdrant-client==1.9.1               # Qdrant Python client

# ─── AWS ──────────────────────────────────────────────────────
boto3==1.34.130
botocore==1.34.130
aiobotocore==2.13.0                # Async AWS

# ─── SECURITY + ENCRYPTION ────────────────────────────────────
python-jose[cryptography]==3.3.0   # JWT
passlib[bcrypt]==1.7.4             # Password hashing
cryptography==42.0.8               # AES encryption
hvac==2.3.0                        # HashiCorp Vault client
pyotp==2.9.0                       # TOTP for MFA

# ─── AI/ML FRAMEWORKS ─────────────────────────────────────────
torch==2.3.0                       # PyTorch (TFT model)
torchvision==0.18.0
onnxruntime==1.18.0                # ONNX model inference (faster)
transformers==4.41.1               # HuggingFace (BioMedBERT)
sentence-transformers==3.0.1       # Patient embeddings
pytorch-forecasting==1.1.1         # TFT implementation
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.5.0
scipy==1.13.1

# ─── NLP ──────────────────────────────────────────────────────
spacy==3.7.5                       # NLP preprocessing
# en_core_web_sm installed separately: python -m spacy download en_core_web_sm
scispacy==0.5.3                    # Biomedical NLP
# Note: download biomedical NER model separately
# pip install https://s3-us-west-2.amazonaws.com/ai2-s2-scispacy/releases/v0.5.3/en_core_sci_lg-0.5.3.tar.gz

# ─── MEDICAL IMAGING ──────────────────────────────────────────
monai==1.3.1                       # Medical imaging AI framework
pydicom==2.4.4                     # DICOM file parsing
pillow==10.3.0                     # Image processing
opencv-python-headless==4.9.0.80

# ─── HL7 PARSING ──────────────────────────────────────────────
hl7==0.4.5                         # HL7 v2 message parsing
lxml==5.2.2                        # XML parsing (CDA documents)

# ─── DATA PIPELINE ────────────────────────────────────────────
apache-airflow==2.9.1              # Batch ETL orchestration
apache-airflow-providers-postgres==5.10.2
apache-airflow-providers-amazon==8.20.0
pyarrow==16.1.0                    # Apache Parquet

# ─── MONITORING ───────────────────────────────────────────────
prometheus-client==0.20.0
opentelemetry-api==1.24.0
opentelemetry-sdk==1.24.0
opentelemetry-instrumentation-fastapi==0.45b0
sentry-sdk[fastapi]==2.5.1

# ─── UTILITIES ────────────────────────────────────────────────
python-dotenv==1.0.1
tenacity==8.3.0                    # Retry logic
structlog==24.2.0                  # Structured logging
rich==13.7.1                       # CLI output
click==8.1.7                       # CLI framework
pyyaml==6.0.1
toml==0.10.2
ujson==5.10.0                      # Fast JSON
orjson==3.10.3                     # Even faster JSON
msgpack==1.0.8                     # Binary serialization for Kafka

# ─── DATE/TIME ────────────────────────────────────────────────
python-dateutil==2.9.0
pytz==2024.1
arrow==1.3.0

# ─── TESTING ──────────────────────────────────────────────────
pytest==8.2.1
pytest-asyncio==0.23.7
pytest-cov==5.0.0
pytest-mock==3.14.0
httpx==0.27.0                      # HTTP client for testing
factory-boy==3.3.0                 # Test fixtures
faker==25.3.0                      # Fake data generation
freezegun==1.5.1                   # Time mocking

# ─── CODE QUALITY ─────────────────────────────────────────────
black==24.4.2
ruff==0.4.7
mypy==1.10.0
pre-commit==3.7.1
bandit==1.7.9                      # Security linting

# ─── REPORT GENERATION ────────────────────────────────────────
weasyprint==62.3                   # PDF generation (CFO reports)
jinja2==3.1.4                      # HTML templates

# ─── FEDERATED LEARNING ───────────────────────────────────────
# nvflare==2.4.1                   # Uncomment when multi-hospital
# syft==0.8.7                      # PySyft alternative
